<?xml version="1.0" encoding="utf-8" ?>
<ContentPage Title="Meus tickets" xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MT.MeusTickets">
    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="plus.png" Text="Adicionar"
                     Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>
    <ListView x:Name="ticketsListView">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Descricao}"></TextCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>